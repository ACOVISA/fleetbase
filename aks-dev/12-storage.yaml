apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: fleetbase-storage-app
  namespace: fleetbase
spec:
  accessModes: ["ReadWriteMany"]
  storageClassName: azurefile-csi
  resources:
    requests:
      storage: 20Gi
